query FindAllStreams($filters: FiltersInput!) {
  findAllStreams(filters: $filters) {
    id
    title
    thumbnailUrl
    isLive
    user {
      id
      username
      avatar
      isVerified
    }
    category {
      id
      title
      slug
    }
  }
}
