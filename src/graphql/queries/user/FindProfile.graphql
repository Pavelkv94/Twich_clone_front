query FindProfile {
  getMe {
    username
    email
    displayName
    avatar
    bio
    isTotpEnabled
    notificationSettings {
      siteNotification
      telegramNotification
    }
    stream {
      serverUrl
      streamKey
    }
  }
}
