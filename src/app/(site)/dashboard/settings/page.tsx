import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Settings | Dashboard",
}
export default function DashboardSettingsPage() {
    return <div>DashboardSettingsPage</div>
}